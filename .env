# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL=postgresql://neondb_owner:npg_rfTayDW8HV4c@ep-still-heart-anoq5jso-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Set the `NODE_ENV` environment variable to `production` to disable certain development features and enable optimizations.
NODE_ENV=development
# JWT secret key and expiration time for token generation for our utilities/generateToken.js file
JWT_SECRET=0bF5muh3EWCTpRNIzwtky/U7Ida8jX6CiDXD/KS6JEY=
JWT_EXPIRES_IN= 7d

//Testing User Only
USER_ID=767da406-f01f-4735-a463-9e8addb1cded